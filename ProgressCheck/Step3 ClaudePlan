Claude’s Plan
PromptToolKit — 现状分析与下一步规划
Context
基于产品策划书 V6 与当前代码进度（Step1 + Step2），分析实现差距，识别架构问题，规划 Phase 1→2 过渡路径。

现状总结
已完成（Step1 — Phase 1 MVP 基础层）
功能	状态
Tauri + React + TS + SQLite 单仓架构	✅
Prompt 自由文本编辑器 (EditorWorkspace)	✅
版本历史面板 (VersionHistoryPanel)	✅
Diff 面板 (DiffPanel)	✅
片段库面板 (SnippetLibraryPanel) + prefix/suffix/free scope	✅
参考管理器面板 (ReferenceManagerPanel)	✅
产出 Inbox 面板 (OutputInboxPanel)	✅
AI 设置面板 (AISettingsPanel)	✅
一键复制 + 零宽度隐形元数据 payload	✅
粘贴回自动归因（top-3 ranking，30 分钟窗口）	✅
片段拼接预览（prefix → free → editor → suffix）	✅
Windows Credential Manager 密钥存储	✅
14 表初始 SQL schema + migrations	✅
GitHub Actions CI	✅
已完成（Step2 — AI 集成层）
功能	状态
AI client layer（4 provider：openai_compatible / openai / anthropic / gemini）	✅
prompt_generate_from_brief 命令	✅
prompt_rewrite_candidates（3 候选 + unified diff）	✅
AI Run 持久化（状态/延迟/错误记录）	✅
AI Run 历史展示	✅
AI 默认 provider 管理	✅
EditorWorkspace AI Toolbar（生成/改写 UI）	✅
HTTP mock 集成测试（14 个 passing）	✅
架构问题
问题 1：AI Toolbar 定位与策划书冲突
现状：Step2 把 "generate from brief" 和 "rewrite" 直接集成进 EditorWorkspace AI Toolbar。

策划书要求（§6）：

扩展工具页（Prompt 生成器、图片分析器、Moodboard 分析器）不占据主界面，用户需要时打开，产出流入主工作流。
主工作流编辑器里只保留修改器（§6.2 Prompt 修改器），不是生成器。
实际差距：

"generate from brief" = Prompt 生成器（§6.11）→ 应是扩展工具页
"rewrite" = 语义修改器（§6.2）→ 应在主编辑器内，但需要支持选区改写
后果：目前主界面功能边界模糊，会稀释核心体验（正是策划书强调要避免的）。

问题 2：改写器缺少选区改写（Selection Rewrite）
策划书核心设计（§6.2）：

用户选中 prompt 中的一段文本，只对选中部分应用修改，而不是整段 prompt 重写。这是最重要的交互设计。

现状：prompt_rewrite_candidates 对整段 prompt 做改写，没有 selection 参数。

问题 3：改写候选类型缺失
策划书要求 3 候选应区分保守 / 中等 / 激进，当前实现是 3 个等价候选，没有层次区分。

问题 4：片段合并整理功能缺失
策划书（§6.6）：合并整理按钮 → AI 去重/排序/冲突标记/不扩写/给 diff 预览。当前 snippet scope 拼接存在，但无合并整理逻辑。

问题 5：项目全局后缀 UI 缺失
策划书（§6.6/6.8）：项目层设置 suffix scope 文本，追加到该项目所有 prompt 末尾。数据库可能有字段，但无明确 UI 路径。

问题 6：参考管理器标签扩展性
策划书（§6.5）：底层 tag system 支持用户自由添加标签。需确认当前实现是否只有固定 3 类（style/character/structure）还是已支持自定义 tag。

下一步规划（Phase 1 完善 + Phase 2 起步）
Step 3 — 架构修正 + Phase 1 完善（高优先级）
目标：在进入 Phase 2 扩展功能前，先把架构问题修正，避免后续债务累积。

拆分 AI Toolbar

将 "generate from brief" 移出 EditorWorkspace，独立为 PromptGeneratorPanel（扩展工具页）
EditorWorkspace 只保留语义修改器入口（简化 toolbar）
修改器产出直接流入编辑器，Prompt Generator 输出到编辑器（与策划书 §6 一致）
选区改写支持

prompt_rewrite_candidates 增加 selection: Option<(usize, usize)> 参数
前端编辑器支持获取选区范围并传递
只改写选中部分，其余文本原样保留
候选分层（保守/中等/激进）

AI prompt 系统增加 3 个 instruction 变体，明确指示改写程度
前端 diff 展示标注 conservative / moderate / aggressive 标签
参考管理器自定义标签

确认并修复 tag system，支持用户自由增删标签
默认展示 style/character/structure 作为快捷入口
项目全局后缀 UI

ProjectSettings 面板增加 global suffix 输入区
拼接预览时自动追加
Step 4 — Phase 2 扩展工具页（次优先）
图片分析器（ImageAnalyzerPanel）

VLM 调用：多维度分析（构图/光影/相机/色彩/风格）
Core + Detail 两层输出
置信度标注
反垃圾过滤（masterpiece/best quality 等）
勾选维度 → 一键插入编辑器或保存为片段
Moodboard 分析器（MoodboardPanel）

批量图片（5-20 张）→ 共性提炼
输出可直接设为项目全局后缀或保存为 suffix 片段
片段合并整理

拼接后 LLM 调用：去重/排序/冲突标记
不扩写，给 diff 预览
语义冲突检测

改写时自动检测冲突词对（如 close-up + medium shot）
应用前弹出警告
跨模型产出对比视图

OutputInboxPanel 增强：同一 prompt 版本 → 多模型并排展示
需要澄清的问题
在进入 Step 3 之前，有一个关键架构决策需要确认：

EditorWorkspace 中的 AI Toolbar 处理方式：

方案 A：完全拆出，"generate from brief" 移到独立 Panel，主编辑器只保留修改器
方案 B：保留 Toolbar 但改版，把 "generate" 隐藏到扩展面板入口，Toolbar 只保留 "rewrite/modify"
验证方式
TypeScript lint + Vite build 通过
Cargo check + cargo test（现有 14 tests 继续 passing）
手动测试：选区改写 → diff 预览 → 应用候选
手动测试：图片上传 → 分析 → 勾选维度 → 插入编辑器
Stayed in plan mode
很好的设计洞察。这其实是 Prompt Generator 的核心 UX 问题：brief 太短时，生成的 prompt 会很泛。

你说的方向是：生成前先走一个维度补充流程，让用户知道哪些维度缺失，可以填入也可以跳过。我来更新计划加入这个设计。

Edit C:\Users\kerqc\.claude\plans\peppy-stargazing-blossom.md
Added 41 lines
Claude’s Plan
PromptToolKit — 现状分析与下一步规划
Context
基于产品策划书 V6 与当前代码进度（Step1 + Step2），分析实现差距，识别架构问题，规划 Phase 1→2 过渡路径。

现状总结
已完成（Step1 — Phase 1 MVP 基础层）
功能	状态
Tauri + React + TS + SQLite 单仓架构	✅
Prompt 自由文本编辑器 (EditorWorkspace)	✅
版本历史面板 (VersionHistoryPanel)	✅
Diff 面板 (DiffPanel)	✅
片段库面板 (SnippetLibraryPanel) + prefix/suffix/free scope	✅
参考管理器面板 (ReferenceManagerPanel)	✅
产出 Inbox 面板 (OutputInboxPanel)	✅
AI 设置面板 (AISettingsPanel)	✅
一键复制 + 零宽度隐形元数据 payload	✅
粘贴回自动归因（top-3 ranking，30 分钟窗口）	✅
片段拼接预览（prefix → free → editor → suffix）	✅
Windows Credential Manager 密钥存储	✅
14 表初始 SQL schema + migrations	✅
GitHub Actions CI	✅
已完成（Step2 — AI 集成层）
功能	状态
AI client layer（4 provider：openai_compatible / openai / anthropic / gemini）	✅
prompt_generate_from_brief 命令	✅
prompt_rewrite_candidates（3 候选 + unified diff）	✅
AI Run 持久化（状态/延迟/错误记录）	✅
AI Run 历史展示	✅
AI 默认 provider 管理	✅
EditorWorkspace AI Toolbar（生成/改写 UI）	✅
HTTP mock 集成测试（14 个 passing）	✅
架构问题
问题 1：AI Toolbar 定位与策划书冲突
现状：Step2 把 "generate from brief" 和 "rewrite" 直接集成进 EditorWorkspace AI Toolbar。

策划书要求（§6）：

扩展工具页（Prompt 生成器、图片分析器、Moodboard 分析器）不占据主界面，用户需要时打开，产出流入主工作流。
主工作流编辑器里只保留修改器（§6.2 Prompt 修改器），不是生成器。
实际差距：

"generate from brief" = Prompt 生成器（§6.11）→ 应是扩展工具页
"rewrite" = 语义修改器（§6.2）→ 应在主编辑器内，但需要支持选区改写
后果：目前主界面功能边界模糊，会稀释核心体验（正是策划书强调要避免的）。

问题 2：改写器缺少选区改写（Selection Rewrite）
策划书核心设计（§6.2）：

用户选中 prompt 中的一段文本，只对选中部分应用修改，而不是整段 prompt 重写。这是最重要的交互设计。

现状：prompt_rewrite_candidates 对整段 prompt 做改写，没有 selection 参数。

问题 3：改写候选类型缺失
策划书要求 3 候选应区分保守 / 中等 / 激进，当前实现是 3 个等价候选，没有层次区分。

问题 4：片段合并整理功能缺失
策划书（§6.6）：合并整理按钮 → AI 去重/排序/冲突标记/不扩写/给 diff 预览。当前 snippet scope 拼接存在，但无合并整理逻辑。

问题 5：项目全局后缀 UI 缺失
策划书（§6.6/6.8）：项目层设置 suffix scope 文本，追加到该项目所有 prompt 末尾。数据库可能有字段，但无明确 UI 路径。

问题 6：参考管理器标签扩展性
策划书（§6.5）：底层 tag system 支持用户自由添加标签。需确认当前实现是否只有固定 3 类（style/character/structure）还是已支持自定义 tag。

下一步规划（Phase 1 完善 + Phase 2 起步）
Step 3 — 架构修正 + Phase 1 完善（高优先级）
目标：在进入 Phase 2 扩展功能前，先把架构问题修正，避免后续债务累积。

拆分 AI Toolbar

将 "generate from brief" 移出 EditorWorkspace，独立为 PromptGeneratorPanel（扩展工具页）
EditorWorkspace 只保留语义修改器入口（简化 toolbar）
修改器产出直接流入编辑器，Prompt Generator 输出到编辑器（与策划书 §6 一致）
选区改写支持

prompt_rewrite_candidates 增加 selection: Option<(usize, usize)> 参数
前端编辑器支持获取选区范围并传递
只改写选中部分，其余文本原样保留
候选分层（保守/中等/激进）

AI prompt 系统增加 3 个 instruction 变体，明确指示改写程度
前端 diff 展示标注 conservative / moderate / aggressive 标签
参考管理器自定义标签

确认并修复 tag system，支持用户自由增删标签
默认展示 style/character/structure 作为快捷入口
项目全局后缀 UI

ProjectSettings 面板增加 global suffix 输入区
拼接预览时自动追加
Step 3.5 — Prompt Generator 交互设计（重要补充）
策划书 §6.11 定位 Prompt Generator 为"新手入口和快速原型工具"，但当前 "generate from brief" 的问题是：
用户只写一句 brief，AI 会生成泛化、无特色的 prompt（缺相机、光线、材质等维度）。

设计方案：维度补充引导流程


Step 1  用户输入 brief（自然语言，1-3 句）
          "a warrior standing in fog"

Step 2  系统展示"维度补充"面板
          ┌─────────────────────────────────────┐
          │ 以下维度未指定，填入可让 prompt 更完整   │
          │ 也可以跳过，AI 将自动补充泛化描述       │
          ├──────────────┬──────────────────────┤
          │ 📷 相机/镜头  │ [输入 / 跳过]         │
          │ 💡 光线       │ [输入 / 跳过]         │
          │ 🎨 风格/氛围  │ [输入 / 跳过]         │
          │ 🧱 材质/质感  │ [输入 / 跳过]         │
          │ 🖼  构图      │ [输入 / 跳过]         │
          └──────────────┴──────────────────────┘

Step 3  用户填入部分维度 / 全部跳过

Step 4  点击"生成" → 输出自然语言 prompt → 送入主编辑器
关键设计原则：

维度补充是可选引导，不是强制表单
全部跳过时，AI 仍会生成（但提示"建议补充更多维度以获得更精准结果"）
填入的维度内容也是自然语言，不是下拉选项（例如相机：用户可写 "portrait, 85mm feel" 而不是选 "85mm"）
输出始终是自然语言 prompt，不是字段列表
与片段系统联动：已有的 prefix/suffix 片段可在此自动注入上下文
后端实现：

prompt_generate_from_brief 命令增加 dimensions: HashMap<String, Option<String>> 参数
AI prompt 模板区分：用户填写的维度 vs 跳过的维度（跳过时告知 AI 可自由发挥，填写时严格遵守）
Step 4 — Phase 2 扩展工具页（次优先）
图片分析器（ImageAnalyzerPanel）

VLM 调用：多维度分析（构图/光影/相机/色彩/风格）
Core + Detail 两层输出
置信度标注
反垃圾过滤（masterpiece/best quality 等）
勾选维度 → 一键插入编辑器或保存为片段
Moodboard 分析器（MoodboardPanel）

批量图片（5-20 张）→ 共性提炼
输出可直接设为项目全局后缀或保存为 suffix 片段
片段合并整理

拼接后 LLM 调用：去重/排序/冲突标记
不扩写，给 diff 预览
语义冲突检测

改写时自动检测冲突词对（如 close-up + medium shot）
应用前弹出警告
跨模型产出对比视图

OutputInboxPanel 增强：同一 prompt 版本 → 多模型并排展示
需要澄清的问题
在进入 Step 3 之前，有一个关键架构决策需要确认：

EditorWorkspace 中的 AI Toolbar 处理方式：

方案 A：完全拆出，"generate from brief" 移到独立 Panel，主编辑器只保留修改器
方案 B：保留 Toolbar 但改版，把 "generate" 隐藏到扩展面板入口，Toolbar 只保留 "rewrite/modify"
验证方式
TypeScript lint + Vite build 通过
Cargo check + cargo test（现有 14 tests 继续 passing）
手动测试：选区改写 → diff 预览 → 应用候选
手动测试：图片上传 → 分析 → 勾选维度 → 插入编辑器